# 🎓 University DataHub - Backend API

**Комплексная платформа для каталогизации университетов Казахстана**

![Python](https://img.shields.io/badge/Python-3.12-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.119-green)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue)
![Docker](https://img.shields.io/badge/Docker-Ready-blue)

## 🌟 Особенности

- ✅ **5 университетов** с реальными данными (Satbayev, КазНУ, KIMEP, AlmaU, МУИТ)
- ✅ **15+ программ** обучения с ценами и условиями
- ✅ **Умный поиск** с фильтрацией по городу, цене, рейтингу
- ✅ **Сравнение университетов** side-by-side
- ✅ **Система избранного** для пользователей
- ✅ **JWT авторизация** с ролями (admin/user)
- ✅ **Гранты, общежития, партнерства**
- ✅ **Статистика** для дашборда
- ✅ **Docker** для быстрого развертывания

## 🛠 Технологический стек

| Компонент | Технология |
|-----------|-----------|
| **Backend** | FastAPI (Python 3.12) |
| **Database** | PostgreSQL 16 + asyncpg |
| **ORM** | SQLAlchemy 2.0 (async) |
| **Миграции** | Alembic |
| **Авторизация** | JWT + BCrypt |
| **Инфраструктура** | Docker Compose |

## ⚡ Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd University_DataHub_Backend
```

### 2. Создание .env файла

```env
# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_DB=university_db
DB_HOST=db
DB_PORT=5432

# Security
SECRET_KEY=your_super_secret_key_change_me_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### 3. Запуск через Docker (рекомендуется)

```bash
# Запустить контейнеры
docker-compose up --build -d

# Дождаться готовности базы (5-10 секунд)

# Применить миграции
docker-compose exec backend alembic upgrade head

# Загрузить тестовые данные (5 университетов)
docker-compose exec backend python -m scripts.seed_data
```

### 4. Проверка работы

Открой: **http://localhost:8000/docs**

**Тестовые учетные данные:**
- **Admin**: `admin@university.kz` / `admin123`
- **Student**: `student@gmail.com` / `student123`

## 📚 Структура базы данных

```
┌─────────────┐      ┌──────────────┐      ┌──────────┐
│ Universities│◄─────┤  Programs    │      │  Users   │
│             │      │              │      │          │
│ - name      │      │ - name       │      │ - email  │
│ - city      │      │ - price      │      │ - role   │
│ - rating    │      │ - degree     │      └──────────┘
└─────────────┘      └──────────────┘           │
      │                                          │
      ├──────► Faculties                         │
      ├──────► Grants                   ┌────────▼────────┐
      ├──────► Dormitories             │   Favorites     │
      ├──────► Partnerships            └─────────────────┘
      └──────► Admissions
```

### Основные таблицы (10 штук):

1. **users** - Пользователи системы
2. **universities** - Университеты (40+ полей)
3. **programs** - Образовательные программы
4. **faculties** - Факультеты
5. **departments** - Кафедры
6. **grants** - Гранты и стипендии
7. **dormitories** - Общежития
8. **partnerships** - Международное сотрудничество
9. **favorites** - Избранные университеты
10. **admissions** - Условия поступления

## 🔑 API Эндпоинты

### 📊 Статистика

```http
GET /universities/stats
```

Возвращает:
- Общее количество университетов
- Количество программ
- Средняя цена обучения
- Топ-5 университетов по рейтингу

### 🏛 Университеты

```http
GET    /universities/                    # Список с фильтрами
GET    /universities/{id}                # Детальная информация
POST   /universities/                    # Создать (admin)
PATCH  /universities/{id}                # Обновить (admin)
DELETE /universities/{id}                # Удалить (admin)
POST   /universities/compare             # Сравнение
```

**Параметры фильтрации:**
- `?city=Алматы` - по городу
- `?has_dormitory=true` - с общежитием
- `?min_rating=4.0` - минимальный рейтинг
- `?max_price=2000000` - максимальная цена программ
- `?query=IT` - поиск по названию/описанию

### 📖 Программы

```http
GET  /universities/programs/search       # Поиск программ
POST /universities/{id}/programs         # Добавить программу (admin)
```

**Фильтры поиска программ:**
- `?degree=bachelor` - степень (bachelor/master/phd)
- `?min_price=500000` - минимальная цена
- `?max_price=2000000` - максимальная цена
- `?city=Алматы` - город
- `?query=программирование` - текстовый поиск

### 🏫 Дополнительные данные

```http
POST /universities/{id}/faculties        # Добавить факультет
POST /universities/{id}/grants           # Добавить грант
GET  /universities/{id}/grants           # Список грантов
POST /universities/{id}/dormitories      # Добавить общежитие
POST /universities/{id}/admissions       # Добавить условия поступления
GET  /universities/{id}/admissions       # Получить условия поступления
```

### ⭐ Избранное

```http
POST   /universities/{id}/favorite       # Добавить в избранное
DELETE /universities/{id}/favorite       # Удалить из избранного
GET    /universities/favorites/my        # Мои избранные
```

### 🔐 Авторизация

```http
POST /auth/register                      # Регистрация
POST /auth/login                         # Вход (получение токена)
GET  /auth/me                            # Текущий пользователь
```

## 🔍 Примеры использования

### Поиск университетов

```bash
# С общежитием в Алматы, рейтинг > 4.0
curl "http://localhost:8000/universities/?city=Алматы&has_dormitory=true&min_rating=4.0"

# По названию
curl "http://localhost:8000/universities/?query=Satbayev"

# С программами до 1.5 млн
curl "http://localhost:8000/universities/?max_price=1500000"
```

### Поиск программ

```bash
# Бакалавриат до 1.5 млн тенге
curl "http://localhost:8000/universities/programs/search?degree=bachelor&max_price=1500000"

# IT-программы в Алматы
curl "http://localhost:8000/universities/programs/search?city=Алматы&query=информац"
```

### Сравнение университетов

```bash
curl -X POST "http://localhost:8000/universities/compare" \
  -H "Content-Type: application/json" \
  -d '{"university_ids": [1, 2, 3]}'
```

### Авторизация

```bash
# Вход
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@university.kz&password=admin123"

# Использование токена
curl "http://localhost:8000/auth/me" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

## 📊 Что включено в тестовые данные

### 1. **Satbayev University** (Технический)
- 3 программы (IT: Информационные системы, ВТ и ПО)
- Факультет IT
- Государственные гранты
- Общежитие
- Партнерство с TU Berlin

### 2. **КазНУ им. аль-Фараби** (Классический)
- 3 программы (Физика, Математика, Международные отношения)
- Механико-математический факультет
- Грант Президента
- Студенческий городок

### 3. **KIMEP University** (Бизнес, международный)
- 2 программы (BBA, MBA)
- Обучение на английском
- Высокий процент трудоустройства (92%)

### 4. **AlmaU** (Бизнес)
- 2 программы (Менеджмент, Маркетинг)
- Первая бизнес-школа Казахстана

### 5. **МУИТ** (IT)
- 3 программы (Разработка ПО, Кибербезопасность, Data Science)
- Партнерство с Университетом Иннополис

## 🚀 Roadmap для фронтенда

### Этап 1: Базовая разработка (React)
- [ ] Настроить Vite + React + TypeScript
- [ ] Подключить TailwindCSS / Ant Design
- [ ] Создать главную страницу
- [ ] Реализовать каталог университетов
- [ ] Добавить фильтры и поиск

### Этап 2: Детальные страницы
- [ ] Страница университета (табы: обзор, программы, поступление)
- [ ] Карточки программ
- [ ] 3D туры (iframe)
- [ ] Галерея фотографий

### Этап 3: Интерактивные функции
- [ ] Авторизация (JWT)
- [ ] Избранное
- [ ] Сравнение университетов (таблица)
- [ ] Личный кабинет

### Этап 4: Продвинутые фичи
- [ ] Рекомендации на основе предпочтений
- [ ] Чат-бот консультант
- [ ] Многоязычность (RU/KZ/EN)
- [ ] Адаптивный дизайн

## 🐛 Решение проблем

**База данных не поднимается:**
```bash
docker-compose down -v
docker-compose up --build
```

**Ошибка миграций:**
```bash
docker-compose exec backend alembic downgrade -1
docker-compose exec backend alembic upgrade head
```

**Пересоздать данные:**
```bash
docker-compose down -v
docker-compose up -d
sleep 10
docker-compose exec backend alembic upgrade head
docker-compose exec backend python -m scripts.seed_data
```

**Проверить логи:**
```bash
docker-compose logs -f backend
```

## 📁 Структура проекта

```
University_DataHub_Backend/
├── app/
│   ├── core/              # Конфигурация, security
│   │   ├── config.py      # Настройки из .env
│   │   └── security.py    # JWT, password hashing
│   ├── db/                # База данных
│   │   ├── database.py    # Подключение, сессии
│   │   └── models.py      # SQLAlchemy модели
│   ├── routers/           # API эндпоинты
│   │   ├── auth.py        # Регистрация/вход
│   │   └── universities.py # CRUD университетов
│   ├── schemas/           # Pydantic схемы
│   │   ├── user.py
│   │   └── university.py
│   ├── dependencies.py    # JWT авторизация
│   └── main.py            # Точка входа FastAPI
├── scripts/
│   └── seed_data.py       # Загрузка тестовых данных
├── alembic/               # Миграции
│   └── versions/
├── .env                   # Переменные окружения
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile
├── requirements.txt       # Зависимости Python
└── README.md
```

## 👨‍💻 Для разработчиков

### Создание новой миграции

```bash
docker-compose exec backend alembic revision --autogenerate -m "описание"
docker-compose exec backend alembic upgrade head
```

### Подключение к базе

```bash
docker-compose exec db psql -U postgres -d university_db
```

### Локальная разработка (без Docker)

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

pip install -r requirements.txt

# Создать базу
createdb university_db

# В .env указать DB_HOST=localhost

alembic upgrade head
python -m scripts.seed_data

uvicorn app.main:app --reload
```

## 📄 Лицензия

MIT License

## 🤝 Команда

Создано для **ITU HACKATHON-2025**

---

**⚡ Backend готов! Переходим к фронтенду! 🚀**

**Следующий шаг:** Создание React-приложения в отдельном репозитории
